#ifndef __SECRETS_H__
#define __SECRETS_H__

#include <Arduino.h>

/*
 * Kivsee variant (esp32c3_kivsee) secrets. Copy this file to secrets.h and fill
 * in the 'xxxx' values. secrets.h is gitignored. Only the kivsee build uses it.
 */

#define SSID "xxxx"
#define WIFI_PASSWORD "xxxx"        // for no password, use ""
#define TIME_SERVER_PORT 123

#endif // __SECRETS_H__
